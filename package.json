{
  "name": "revision-app-supabase-vercel",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm --prefix frontend install",
    "start": "npm --prefix frontend start",
    "build": "npm --prefix frontend build"
  }
}
